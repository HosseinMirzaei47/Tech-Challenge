package com.example.app.ui.theme

import androidx.compose.material.Colors
import androidx.compose.runtime.Composable
import androidx.compose.ui.graphics.Color

val Colors.PrimaryIndigo30: Color
    @Composable
    get() = if (isLight) LightColors.PrimaryIndigo30 else DarkColors.PrimaryIndigo30

val Colors.PrimaryIndigo40: Color
    @Composable
    get() = if (isLight) LightColors.PrimaryIndigo40 else DarkColors.PrimaryIndigo40

val Colors.PrimaryIndigo50: Color
    @Composable
    get() = if (isLight) LightColors.PrimaryIndigo50 else DarkColors.PrimaryIndigo50

val Colors.PrimaryIndigo60: Color
    @Composable
    get() = if (isLight) LightColors.PrimaryIndigo60 else DarkColors.PrimaryIndigo60

val Colors.PrimaryIndigo80: Color
    @Composable
    get() = if (isLight) LightColors.PrimaryIndigo80 else DarkColors.PrimaryIndigo80

val Colors.PrimaryIndigo100: Color
    @Composable
    get() = if (isLight) LightColors.PrimaryIndigo100 else DarkColors.PrimaryIndigo100

val Colors.Secondary50: Color
    @Composable
    get() = if (isLight) LightColors.Secondary50 else DarkColors.Secondary50

val Colors.Secondary70: Color
    @Composable
    get() = if (isLight) LightColors.Secondary70 else DarkColors.Secondary70

val Colors.Background: Color
    @Composable
    get() = if (isLight) LightColors.Background else DarkColors.Background

val Colors.Success30: Color
    @Composable
    get() = if (isLight) LightColors.Success30 else DarkColors.Success30

val Colors.Success50: Color
    @Composable
    get() = if (isLight) LightColors.Success50 else DarkColors.Success50

val Colors.Success70: Color
    @Composable
    get() = if (isLight) LightColors.Success70 else DarkColors.Success70

val Colors.Success100: Color
    @Composable
    get() = if (isLight) LightColors.Success100 else DarkColors.Success100

val Colors.Error30: Color
    @Composable
    get() = if (isLight) LightColors.Error30 else DarkColors.Error30

val Colors.Error50: Color
    @Composable
    get() = if (isLight) LightColors.Error50 else DarkColors.Error50

val Colors.Error70: Color
    @Composable
    get() = if (isLight) LightColors.Error70 else DarkColors.Error70

val Colors.Error100: Color
    @Composable
    get() = if (isLight) LightColors.Error100 else DarkColors.Error100

val Colors.Gray30: Color
    @Composable
    get() = if (isLight) LightColors.Gray30 else DarkColors.Gray30

val Colors.Gray50: Color
    @Composable
    get() = if (isLight) LightColors.Gray50 else DarkColors.Gray50

val Colors.Gray70: Color
    @Composable
    get() = if (isLight) LightColors.Gray70 else DarkColors.Gray70

val Colors.Gray100: Color
    @Composable
    get() = if (isLight) LightColors.Gray100 else DarkColors.Gray100

val Colors.Yellow50: Color
    @Composable
    get() = if (isLight) LightColors.Yellow50 else DarkColors.Yellow50

val Colors.TextColor: Color
    @Composable
    get() = if (isLight) LightColors.TextColor else DarkColors.TextColor